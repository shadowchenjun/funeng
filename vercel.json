{
  "buildCommand": "cd frontend && npm run build && cp -r dist ../dist",
  "installCommand": "cd frontend && npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/index.html"
    }
  ]
}
